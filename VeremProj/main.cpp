#include <iostream>
#include <stack>
#include <string>

using namespace std;

int main()
{
	cout << "Lokjed az inputot!" << endl;
/*
	stack<char> s;
	//string expr = "[][]{dfsdff}(fsd[fasdf[]fsf])";
	string expr = "[]";
	bool err = false;
	//cin >> expr;

	for (auto c : expr)
	{
		if (c == '(' || c == '{' || c == '[')
		{
			s.push(c);
			//cout << "into:" << c << endl;
		}
		else if ((c == '(' && s.top() == ')') || (c == '{' &&.top() == '}') || (c == '[' && s.top() == ']'))
		{
			cout << c << endl;
			if (c == s.top())
			{
				cout << "out: " << s.top() << endl;
				s.pop();
			}
			else
			{
				err = true;
				break;
			}
		}
	}

	if (err)
	{
		cout << "hiba" << endl;
		while (!s.empty())
		{
			cout << s.top() << endl;
			s.pop();
		}
	}
	else
	{
		cout << "ok" << endl;
	}
	*/
	system("pause");

	return 0;
}